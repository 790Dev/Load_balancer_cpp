version: "3.9"

services:

  backend1:
    build:
      context: ./backend1
    container_name: backend1
    ports:
      - "5001:5001"

  backend2:
    build:
      context: ./backend2
    container_name: backend2
    ports:
      - "5002:5002"

  backend3:
    build:
      context: ./backend3
    container_name: backend3
    ports:
      - "5003:5003"